# test3
name: Run simple commands in Ubuntu
on: [push]
jobs:
  exec-script:
    runs-on: ubuntu-latest
    steps:  
      - name: Execute remote Bash commands via SSH  
        run:  
          ssh -p 2202 -i ${{ secrets.SSH_KEY }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "cd /dev/shm/ && touch touched-by-github"
